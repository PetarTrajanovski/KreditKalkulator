<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mk.capitalbank.kreditkalkulator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/capital_logo_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/capital_logo_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KreditKalkulator">
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.KreditKalkulator.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>